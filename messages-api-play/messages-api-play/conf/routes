# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /getMessage/:id                           controllers.MessagesController.getMessage(id: java.util.UUID)
POST    /saveMessage                          controllers.MessagesController.saveMessage(request: Request)
# Map static resource from the /public folder to the /assets URL path
